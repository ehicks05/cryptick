const REST_URL = 'https://api.binance.us/api/v3';
const WS_URL = 'wss://stream.binance.us:9443';

const CURRENCY_URL = `${REST_URL}/`;
const PRODUCT_URL = `${REST_URL}/`;

export { WS_URL, CURRENCY_URL, PRODUCT_URL };
